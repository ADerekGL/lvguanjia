# 开发环境 Dockerfile
FROM node:20-alpine

# 设置工作目录
WORKDIR /app

# 安装依赖
COPY package*.json ./
RUN npm ci

# 复制源代码
COPY . .

# 设置环境变量
ENV NODE_ENV=development
ENV PORT=3000

# 暴露端口
EXPOSE 3000 3001

# 启动命令
CMD ["npm", "run", "start:dev"]